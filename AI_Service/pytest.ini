[pytest]
# Tests import the module the same way main.py does (flat: `import config`,
# `from router import router`), so AI_Service/ and tests/ both go on the path.
pythonpath = . tests
testpaths = tests
