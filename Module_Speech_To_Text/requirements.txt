# ─────────────────────────── Persian STT app (app/) ───────────────────────────
# Install for the desktop transcription app:  pip install -r requirements.txt
# For the benchmark/eval tooling instead, use  benchmark/requirements.txt
#
# Version pins are intentionally minimal — only where compatibility matters.

# Core ML / inference
torch>=2.1                # FP16 inference on GPU when available, else CPU
transformers>=4.45        # Whisper large-v3-turbo + SeamlessM4T v2 need this floor
accelerate>=0.26          # faster, lower-memory model loading / device placement
sentencepiece             # tokenizer backend for SeamlessM4T (Persian target = "pes")
protobuf                  # required by the SeamlessM4T / multilingual tokenizers

# Audio I/O
librosa>=0.10             # file loading + resampling (mp3/wav/flac/m4a/ogg/…)
soundfile                 # audio backend used by librosa
sounddevice               # microphone capture for realtime mode
numpy

# Lightweight realtime engine (CPU-only, true streaming)
vosk

# UI: tkinter ships with CPython — no pip package needed
